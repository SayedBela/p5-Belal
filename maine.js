let posX = 0;
let posY = 0;
let diametro =20;
     function setup(){
        createCanvas(600,600);
        background(120,255,50);
      }

function draw(){
    noStroke();

    CSSNumericValue(posX+10,posy+10,diametro);

    posX= posX + 10;
    posY= posY + 10;
    
}